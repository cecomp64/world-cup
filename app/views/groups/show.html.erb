<p id="notice"><%= notice %></p>

<% @teams.each do |team| %>
<p>
  <strong>Team:</strong> <%= team.name %> (<%= team.wins %>, <%= team.loss %>, <%= team.ties %>)
</p>
<% end %>

<%= link_to 'Edit', edit_group_path(@group) %> |
<%= link_to 'Back', groups_path %>
